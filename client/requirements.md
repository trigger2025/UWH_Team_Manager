## Packages
framer-motion | Smooth layout transitions and animations
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes
zod | Schema validation (already in base, but listing for clarity)
lucide-react | Icons (already in base)

## Notes
- Local Storage Only: The app uses `localStorage` for persistence.
- No backend API calls will be made.
- React Query is used to manage local async state for a consistent developer experience.
- Mobile-first design pattern applied.
