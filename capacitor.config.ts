import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.uwh.teammanager',
  appName: 'UWH Team Manager',
  webDir: 'dist/public'
};

export default config;
